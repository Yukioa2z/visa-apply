interface:
  display_name: "Visa Apply"
  short_description: "Research, prepare, and track visa applications."
  default_prompt: "Use $visa-apply to prepare a local visa application dossier for my destination and visa type."

policy:
  allow_implicit_invocation: true
